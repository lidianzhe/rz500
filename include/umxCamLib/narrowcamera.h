#ifndef NARROWCAMERA_H
#define NARROWCAMERA_H

#include "umxcamera.h"

class NarrowCamera : public UMXCamera
{
public:
    NarrowCamera();
    ~NarrowCamera();

public:
//    virtual int getData(char* buf, unsigned int buf_len);
};

#endif // NARROWCAMERA_H
